# File Conventions

- Use Markdown for human/agent records.
- Use YAML for small structured configuration.
- Keep project-owned code and heavy artifacts under `modules/`.
- Keep project memory under `projects/`.
- Do not commit generated caches such as `__pycache__`, `.DS_Store`, scheduler logs, or dependency directories.
- Prefer short, self-contained files over large catch-all documents.
