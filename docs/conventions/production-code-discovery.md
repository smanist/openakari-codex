# Production code discovery

Some repos distinguish “production modules” from research artifacts. If this repo contains a `modules/` directory, treat it as production-grade code unless a module README says otherwise.

## Safety

- Do not open PRs to production modules from autonomous sessions without following the approval process (see `AGENTS.md` and `docs/conventions/governance.md`).

