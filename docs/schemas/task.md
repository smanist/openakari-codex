# Schema: task

Tasks live in `projects/<project>/TASKS.md`.

## Minimal format

```md
- [ ] <imperative verb phrase> [optional tags]
  Why: <motivation>
  Done when: <mechanically verifiable condition>
  Priority: high | medium | low
```

Optional fields:

- `Evidence:` links to relevant artifacts or logs
- `Notes:` constraints / caveats

